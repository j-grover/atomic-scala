// Solution-7.scala

// 7. Create and initialize two Vectors, one containing Ints and one
// containing Doubles. Call the sum, min and max operations on
// each one. 

import com.atomicscala.AtomicTest._

val v1 = Vector(1, 2, 3)
val v2 = Vector(1.5, 2.5, 3.5)

println(v1.sum, v1.min, v1.max)
println(v2.sum, v2.min, v2.max)