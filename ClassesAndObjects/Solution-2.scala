// Solution-2.scala

// 2. Create a String object initialized to "This is an experiment" and call
// the split method on it, passing a space (" ") as the argument to the
// split method. 

val str1 = "This is an experiment"
val str1Split = str1.split(" ")

println(str1Split(0))
println(str1Split(1))
println(str1Split(2))
println(str1Split(3))